# Lösungswege zu den Aufgaben für fortgeschrittene Anwender:innen {#sec-LoesungFortschritt}

::: {.callout-note appearance="simple"}
Wenn Ihr `R`-Code eleganter ist als die hier präsentierten Lösungswege, dann freuen Sie sich! Wenn Sie meinen, Ihr Code sei zu klobig und umständlich, dann Kopf hoch: wenn er tut, was er soll, dann ist er genau richtig.
:::

## Lösungen zu Objekten in R {#sec-LoesungenFortschrittA}

### Lösung zur Aufgabe [-@sec-ACA01] Hogwarts-Kurse {#sec-LCA01}
::: {.callout-tip}
## a) Benutzen Sie die `tribble()`-Funktion, um die Daten in die Objekte `tab1` und `tab2` zu überführen.

```{r}
#| label: LoesungCA01a
#| paged.print: FALSE
library(tibble)
```

:::































## Lösungen zu den Datensatzauswertungen  {#sec-LoesungenFortschrittB}

### Lösung zur Aufgabe [-@sec-ACB01] Hogwarts-Kurse {#sec-LCB01}
::: {.callout-tip}
## a) Benutzen Sie die `tribble()`-Funktion, um die Daten in die Objekte `tab1` und `tab2` zu überführen.

```{r}
#| label: LoesungCB01a
#| paged.print: FALSE
library(tibble)
```

:::

